
## 1. Что такое виртуальные потоки?

- **Виртуальные потоки** — это легковесные потоки, управляемые JVM, а не нативной ОС.  
- Они позволяют писать **высокопроизводительные, масштабируемые приложения,** сохраняя **простоту синхронного стиля программирования.**  
- Противопоставляются **платформенным потокам** (platform threads), которые являются обёрткой над нативными потоками ОС и дороги по ресурсам (стек ~1 МБ на поток).  
- Виртуальные потоки имеют **маленький стек** (начинается с нескольких КБ) и могут создаваться **миллионами.**  
  
> ⚠️ **Важно:** Виртуальные потоки — **не замена** платформенным. Они **не ускоряют CPU-bound задачи.** Они оптимизированы для **I/O-bound** и **блокирующих** операций.  


---

## 2. Создание виртуальных потоков

```java
// Способ 1: через Thread.ofVirtual()
Thread thread = Thread.ofVirtual().name("my-vthread").unstarted(() -> {
    // ваш код
});

thread.start();


// Способ 2
Thread.startVirtualThread(() -> {  
  // ваш код
});


// Способ 3: через Thread.ofVirtual() с запуском
Thread.ofVirtual().start(() -> {  
  // ваш код
});


// Способ 4: через Executors
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    IntStream.range(0, 10_000).forEach(i -> {
        executor.submit(() -> {
            // ваш блокирующий код (например, HTTP-запрос, БД-запрос)
            return i;
        });
    });
} // ждём завершения всех задач
```

- `Executors.newVirtualThreadPerTaskExecutor()` — **рекомендуемый способ** для массового запуска задач.  
- Виртуальные потоки **автоматически уничтожаются** после завершения задачи.  
- Они **не пулуются** — каждый раз создаётся новый.  


---

## 3. Особенности поведения  

🔹 **Не являются демонами** - Виртуальные потоки по умолчанию **не демонические,** но это **не влияет на завершение JVM,** потому что виртуальные потоки **не считаются "живыми"** при проверке условия выхода JVM.  

JVM завершается, когда **все НЕ демонические платформенные потоки завершены, игнорируя виртуальные.**  
  

🔹 **Нельзя сделать joinable или interruptible через Thread API** - Методы `join()` и `interrupt()` работают, но **не рекомендуются.** Лучше использовать `Future, CompletableFuture, StructuredTaskScope.`  


[[☕ 16. Structured Concurrency]]


🔹 **Планирование** - Виртуальные потоки выполняются на **фиксированном пуле платформенных потоков** (carrier threads), называемом **ForkJoinPool.commonPool()** (по умолчанию).  

Это **предотвращает блокировку** — при блокировке I/O виртуальный поток **отсоединяется** от carrier-потока, который может взять другой виртуальный поток.  
  

🔹 **ThreadLocal** - Поддерживаются, **но с накладными расходами.**  
- Использование `ThreadLocal` с виртуальными потоками **может привести к утечкам памяти,** если не очищать значения.  
- **Рекомендация:** избегать `ThreadLocal` или использовать `try-finally` для очистки. 
- Вместо ThreadLocal в виртуальных потоках рекомендуется использовать **Scoped Values**


[[☕ 15. Scoped Values]]


---

## 4. Ограничения и Рекомендации

- **Не подходят для CPU-интенсивных задач** — использовать с осторожностью.
- **Внутренние блокировки** (например, `synchronized`) не освобождают underlying поток.
- Вместо этого использовать **`ReentrantLock`** или **перепроектировать архитектуру** (например, акторы, immutable state).
- **Потоки не наследуют контекст** (например, ThreadLocal) от родителя, если не указано иное.
- **Отладка и профилирование** могут быть сложнее из-за динамической миграции.


---

## 5. Поддержка в Spring Framework

- Полная поддержка виртуальных потоков **на уровне реактивного и императивного стека.**  
- Однако **Spring Web MVC (императивный)** по умолчанию использует **платформенные потоки,** но **может работать с виртуальными,** если сервер поддерживает.
- Добавлена **поддержка виртуальных потоков в Web MVC через Tomcat и Jetty.**  
- Новый сервер: **"Simple" server** на основе `HttpServer` из `com.sun.net.httpserver,` **нативно совместимый с виртуальными потоками.**

```yml
# application.yml
server:
  virtual-threads:
    enabled: true
```

> Работает **только с Tomcat 10.1.13+ и Jetty 12+.** 


---

## 6. Spring WebFlux (реактивный) — не нуждается в виртуальных потоках, так как уже неблокирующий.

- Использование виртуальных потоков в Web MVC **позволяет писать блокирующий код с высокой масштабируемостью.**  
- **Hibernate/JPA:** по умолчанию **блокирующий,** но работает с виртуальными потоками. Однако **connection pool** должен быть **асинхронно масштабируемым** (HikariCP и др. поддерживают).


---

## 7. Поддержка веб-серверами (на ноябрь 2025)


### 1. Apache Tomcat  
- **Версия: 10.1.13+** (сентябрь 2023)  
- Поддержка виртуальных потоков через `VirtualThreadExecutor.`  
- Включается через:  
 
```java
  protocol.setExecutor(Executors.newVirtualThreadPerTaskExecutor());
```

- В Spring Boot 3.2+ — автоматически при `server.virtual-threads.enabled=true.`

> 📄 Источник: [Tomcat 10.1 Changelog]([https://tomcat.apache.org/tomcat-10.1-doc/changelog.html](https://tomcat.apache.org/tomcat-10.1-doc/changelog.html "https://tomcat.apache.org/tomcat-10.1-doc/changelog.html"))  


### 2. Jetty
- **Версия: Jetty 12+** (март 2024)  
- Поддержка виртуальных потоков через `QueuedThreadPool` с режимом виртуальных потоков.  
- Конфигурация:  

```java
  Server server = new Server(new QueuedThreadPool(200, 8, 60_000, true));
  // последний параметр: useVirtualThreads = true
```

> 📄 Источник: [Jetty 12 Documentation]([https://eclipse.dev/jetty/documentation/jetty-12/](https://eclipse.dev/jetty/documentation/jetty-12/ "https://eclipse.dev/jetty/documentation/jetty-12/"))  


### 3. Netty
- **Netty НЕ использует виртуальные потоки напрямую,** так как сам по себе **событийно-ориентированный и неблокирующий.**  
- Виртуальные потоки **некорректно интегрируются** с event loop'ами Netty.  
- **Рекомендация:** использовать **реактивный стек** (WebFlux) с Netty, **а не виртуальные потоки.**  
- Попытки обернуть Netty в виртуальные потоки **лишают преимущества неблокирующей архитектуры.**  

> 📄 Источник: [Netty GitHub Discussions]([https://github.com/netty/netty/discussions](https://github.com/netty/netty/discussions "https://github.com/netty/netty/discussions"))  


### 4. Undertow
- **Нет официальной поддержки** виртуальных потоков (на ноябрь 2025).  
- Red Hat (владелец Undertow) сосредоточен на Quarkus и реактивных моделях.  
- Undertow можно **вручную адаптировать,** но нет встроенных средств.  

 
### 5. com.sun.net.httpserver (Simple server)  
- Встроен в JDK.  
- **Полностью совместим** с виртуальными потоками.  
- Используется Spring Boot как **"simple" сервер** при включении виртуальных потоков.  
- Подходит для **микросервисов и легковесных приложений.**  


---

## 8. Что НЕ рекомендуется делать с виртуальными потоками  

1. **Не использовать пулы виртуальных потоков** — они не пулуются, и пулить их бессмысленно.  
2. **Не использовать для CPU-bound задач** — это не даёт выигрыша и может ухудшить производительность.  
3. **Избегать Thread.stop(), Thread.suspend()** — устаревшие и небезопасные.  
4. **Не полагаться на порядок исполнения** — виртуальные потоки **не гарантируют порядок,** как и обычные.  
5. **Не использовать ThreadLocal без очистки** — риск утечек памяти.  


---

## Официальный источник: 

[JEP 444: Virtual Threads]([https://openjdk.org/jeps/444](https://openjdk.org/jeps/444 "https://openjdk.org/jeps/444"))

[Spring Framework 6.0+ Documentation]([https://docs.spring.io/spring-framework/docs/current/reference/html/](https://docs.spring.io/spring-framework/docs/current/reference/html/ "https://docs.spring.io/spring-framework/docs/current/reference/html/"))

[Spring Boot 3.2 Release Notes]([https://github.com/spring-projects/spring-boot/releases/tag/v3.2.0](https://github.com/spring-projects/spring-boot/releases/tag/v3.2.0 "https://github.com/spring-projects/spring-boot/releases/tag/v3.2.0"))

