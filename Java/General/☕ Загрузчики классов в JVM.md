
##  🔹 1. Введение

**Загрузчик классов (Class Loader)** — это компонент Java Virtual Machine (JVM), отвечающий за **загрузку, линковку и инициализацию** классов и интерфейсов во время выполнения программы.  
  
В отличие от компилируемых языков (например, C++), где весь код линкуется на этапе компиляции, **Java динамически загружает классы по мере необходимости.** Это позволяет делать JVM гибкой, безопасной и расширяемой.  


---
  
## 🔹 2. Жизненный цикл класса в JVM  
  
Класс проходит три фазы:  
  
1. **Loading (Загрузка)**    
   - Поиск и чтение байт-кода класса (обычно из `.class` файла).  
   - Создание объекта `java.lang.Class.`  
  
2. **Linking (Линковка)**    
   - **Verification:** проверка корректности байт-кода (безопасность).  
   - **Preparation:** выделение памяти под статические поля и установка значений по умолчанию.  
   - **Resolution** (опционально): разрешение символьных ссылок на другие классы.  
  
3. **Initialization (Инициализация)**    
   - Выполнение статических блоков и инициализация статических полей явными значениями.  
  
> ⚠️  **Важно:** Загрузчики участвуют **только в первой фазе** — *Loading*. Остальное делает сама JVM.


---
  
## 🔹 3. Иерархия загрузчиков классов  
  
Все загрузчики классов в Java образуют **иерархическую структуру,** основанную на **делегировании (delegation model).** Все загрузчики наследуются от абстрактного класса `java.lang.ClassLoader.`  
  
### 📌 Основные типы загрузчиков:  
  
| Загрузчик классов | Родитель | Где находится | Отвечает за загрузку |  
|-------------------|----------|----------------|------------------------|  
| **Bootstrap ClassLoader** | Отсутствует (`null`) | Реализован на нативном уровне (C/C++), часть JVM | Классы из `JAVA_HOME/jre/lib` (например, `rt.jar,` `java.*`) |  
| **Extension ClassLoader** | Bootstrap | `sun.misc.Launcher$ExtClassLoader` | Классы из `JAVA_HOME/jre/lib/ext` или путей, заданных через `-Djava.ext.dirs` |  
| **Application (System) ClassLoader** | Extension | `sun.misc.Launcher$AppClassLoader` | Классы из classpath (`-cp`, `-classpath,` `CLASSPATH`) |  
| **Custom ClassLoader** | Любой (обычно Application) | Пользовательский | Произвольные источники (сеть, БД, шифрованные файлы и т.д.) |  
  
> 💡  **Bootstrap ClassLoader** не является Java-классом — он **нативный** и **не имеет родителя.** Его родитель в Java — `null.`


---
  
## 🔹 4. Принцип делегирования (Delegation Model)  
  
Это **ключевой принцип** работы загрузчиков:  
  
> Когда загрузчику классов поручают загрузить класс, он **сначала делегирует запрос своему родителю,** и только если родитель не справляется — загружает класс сам.  
  
### 🔗 Порядок поиска класса:  
1. Запрос передаётся **вверх по иерархии** (от текущего загрузчика к Bootstrap).  
2. Если **родитель найдёт класс** — он его загрузит.  
3. Если **никто не нашёл** — текущий загрузчик пытается загрузить класс сам.  
4. Если и он не может — выбрасывается `ClassNotFoundException.`  
  
### ✅ Преимущества:  
- Избегает дублирования классов (например, `java.lang.String` всегда загружается Bootstrap'ом).  
- Повышает безопасность (предотвращает подмену системных классов).  
- Упрощает управление зависимостями. 


---
  
## 🔹 5. Кэширование загруженных классов  
  
Каждый загрузчик **кэширует** загруженные им классы в приватной `Map` (`java.util.Map<String, Class<?>>`).  
  
> 🔒 **Два загрузчика = два разных класса,** даже если байт-код одинаковый!  
  
Пример:

```java
ClassLoader cl1 = new CustomClassLoader();
ClassLoader cl2 = new CustomClassLoader();

Class<?> c1 = cl1.loadClass("com.example.MyClass");
Class<?> c2 = cl2.loadClass("com.example.MyClass");

System.out.println(c1 == c2); // false!
```

Это важно для:  
- Изоляции приложений (например, веб-контейнеры: каждый веб-приложение — свой загрузчик).  
- Hot-reloading (перезагрузка классов без перезапуска JVM).  


---
  
## 🔹 6. Visibility Principle (Принцип видимости)  
  
Класс, загруженный определённым загрузчиком, **видит:**
- Все классы, загруженные **его родителями.**  
- Но **не видит** классы, загруженные **его "детьми"** или "братьями".  
  
Пример:  
- Application ClassLoader загружает `MyApp.class.`  
- `MyApp` может использовать `java.lang.String` (загружен Bootstrap'ом).  
- Но если другой Custom ClassLoader загрузит `Helper.class, MyApp` **не увидит его,** если не будет явно использовать тот же загрузчик.  


---
  
## 🔹 7. Написание собственного загрузчика (Custom ClassLoader)  
  
Для создания своего загрузчика нужно:  
  
1. Унаследоваться от `java.lang.ClassLoader.`  
2. Переопределить метод `findClass(String name).`  
3. **Не переопределять** `loadClass()` без необходимости (чтобы не сломать делегирование!).  
  
### 📌 Пример:

```java
public class MyClassLoader extends ClassLoader {
    private String classPath;

    public MyClassLoader(String classPath) {
        super(ClassLoader.getSystemClassLoader()); // родитель — Application CL
        this.classPath = classPath;
    }

    @Override
    protected Class<?> findClass(String name) throws ClassNotFoundException {
        byte[] classData = loadClassData(name);
        if (classData == null) {
            throw new ClassNotFoundException();
        }
        return defineClass(name, classData, 0, classData.length);
    }

    private byte[] loadClassData(String className) {
        String fileName = classPath + File.separator +
                className.replace('.', File.separatorChar) + ".class";
        try (FileInputStream fis = new FileInputStream(fileName);
             ByteArrayOutputStream baos = new ByteArrayOutputStream()) {
            int data;
            while ((data = fis.read()) != -1) {
                baos.write(data);
            }
            return baos.toByteArray();
        } catch (IOException e) {
            return null;
        }
    }
}
```

> ✅ `defineClass` — это **final native метод,** который превращает `byte[]` в `Class<?>.`  


---
  
## 🔹 8. Практическое применение загрузчиков  
  
- **Веб-серверы / контейнеры (Tomcat, Jetty)**    
  Каждое веб-приложение — отдельный загрузчик. Изоляция контекстов.  
  
- **OSGi (Eclipse, Karaf)**    
  Модульная система с динамической загрузкой/выгрузкой модулей.  
  
- **Hot deployment / Reload классов**    
  Например: JRebel использует кастомные загрузчики для перезагрузки изменённых классов.  
  
- **Плагинные архитектуры**    
  Загрузка плагинов из внешних JAR-файлов во время выполнения.  
  
- **Защита кода**    
  Загрузка классов из зашифрованных файлов или сети (с расшифровкой в загрузчике).  


---
  
## 🔹 9. Важные методы и свойства  
  
| Метод / Поле | Описание |  
|--------------|--------|  
| `ClassLoader.loadClass(String name)` | Публичный метод — вызывает делегирование |  
| `ClassLoader.findClass(String name)` | Переопределяется в кастомных загрузчиках |  
| `ClassLoader.getParent()` | Получить родительский загрузчик |  
| `Class.getClassLoader()` | Узнать, каким загрузчиком был загружен класс |  
| `Thread.getContextClassLoader()` | Загрузчик, ассоциированный с потоком (часто используется в библиотеках, например, JDBC, JNDI) |  
  
> 📌 **System ClassLoader = Application ClassLoader**    
> `ClassLoader.getSystemClassLoader()` возвращает Application ClassLoader.  


---
  
## 🔹 10. Примеры и диагностика  
  
### Как узнать, какой загрузчик загрузил класс:

```java
System.out.println(String.class.getClassLoader());      // null (Bootstrap)
System.out.println(ArrayList.class.getClassLoader());   // null
System.out.println(MyClass.class.getClassLoader());     // AppClassLoader
```

### Как проверить иерархию:

```java
ClassLoader cl = MyClass.class.getClassLoader();
while (cl != null) {
    System.out.println(cl);
    cl = cl.getParent();
}
// Последний — null (Bootstrap)
```


---
  
## 🔹 11. Частые ошибки и проблемы  
  
| Проблема                                   | Причина                                                             | Решение                                                        |     |
| ------------------------------------------ | ------------------------------------------------------------------- | -------------------------------------------------------------- | --- |
|  `ClassNotFoundException`                  | Класс не найден в classpath или не загружен                         | Проверить classpath, делегирование                             |     |
|  `NoClassDefFoundError`                    | Класс был найден при компиляции, но не загружен во время выполнения | Убедиться, что зависимости доступны                            |     |
|  `ClassCastException` при приведении типов | Один и тот же класс загружен разными загрузчиками                   | Использовать общий загрузчик или интерфейсы из общего родителя |     |
| Утечки памяти в веб-приложениях | Классы не выгружаются из-за ссылок на загрузчик | Удалять статические ссылки, закрывать ресурсы |  


---
  
## 🔹 12. Современное состояние (Java 9+)  
  
С появлением **модульной системы (Project Jigsaw)** в Java 9:  
- Bootstrap ClassLoader теперь загружает **модули из `java.*`.**  
- Extension ClassLoader **устарел** (deprecated в Java 8, удалён в Java 9).  
- Появился **Platform ClassLoader** — замена Extension.  
  
Но основной принцип делегирования **остался неизменным.**  


---
  
## 🔹 13. Заключение  
  
- Загрузчики классов — это **фундаментальный механизм динамической загрузки кода** в JVM.  
- Они обеспечивают **безопасность, изоляцию и гибкость.**  
- Понимание их работы критически важно для:  
  - Разработки enterprise-приложений.  
  - Отладки сложных ошибок загрузки классов.  
  - Создания расширяемых систем.  


---
  
## 📚 Полезные ссылки  
  
- [Java Language Specification — Chapter 12: Execution]([https://docs.oracle.com/javase/specs/jls/se17/html/jls-12.html](https://docs.oracle.com/javase/specs/jls/se17/html/jls-12.html "https://docs.oracle.com/javase/specs/jls/se17/html/jls-12.html"))  
- [Java Virtual Machine Specification — Chapter 5: Loading, Linking, and Initializing]([https://docs.oracle.com/javase/specs/jvms/se17/html/jvms-5.html](https://docs.oracle.com/javase/specs/jvms/se17/html/jvms-5.html "https://docs.oracle.com/javase/specs/jvms/se17/html/jvms-5.html"))  
- Oracle Docs: [Understanding the ClassLoader]([https://docs.oracle.com/javase/8/docs/technotes/guides/lang/cl-rel.html](https://docs.oracle.com/javase/8/docs/technotes/guides/lang/cl-rel.html "https://docs.oracle.com/javase/8/docs/technotes/guides/lang/cl-rel.html"))  

