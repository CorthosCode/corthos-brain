
## 🔑 Что такое исключения в Java?

**Исключения (exceptions)** — это объекты, представляющие **ошибочные или исключительные ситуации во время выполнения программы**.
Когда происходит ошибка, Java **создаёт и "выбрасывает" объект исключения** (`throw`), который может быть **обработан (****`catch`****)** или передан выше.


## 🧬 Иерархия исключений

Все исключения — потомки класса `Throwable`:

```shellscript
              Throwable
              /      \
         Error      Exception
                       |
               +---------------+
               |               |
       CheckedException   RuntimeException
```

🔴 Error

* Не подлежат обработке
* Ошибки среды выполнения JVM
* Примеры: `OutOfMemoryError`, `StackOverflowError`

🟠 Exception

* **Обрабатываемые** исключения
* Делятся на:

| Категория               | Описание                                      |
| ----------------------- | --------------------------------------------- |
| ✅ Checked Exceptions    | Компилятор требует обработку (`IOException`)  |
| ⚠️ Unchecked Exceptions | Не требуют обработки (`NullPointerException`) |


## 📦 Примеры исключений

| Категория                  | Примеры                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------------ |
| Checked (обязательные)     | `IOException`, `SQLException`, `ParseException`                                                        |
| Unchecked (необязательные) | `NullPointerException`, `ArithmeticException`, `IllegalArgumentException`, `IndexOutOfBoundsException` |
| Error                      | `OutOfMemoryError`, `StackOverflowError`, `InternalError`                                              |


## 🛠️ Обработка исключений

🔁 try-catch

```java
try {
    // Код, где может произойти исключение
} catch (IOException e) {
    // Обработка IOException
}
```

⚠️ Несколько catch-блоков

```java
try {
    ...
} catch (IOException e) {
    ...
} catch (Exception e) {
    ...
}
```

🧹 finally

```java
try {
    ...
} catch (...) {
    ...
} finally {
    // Всегда выполняется (даже при return/exception)
}
```


## 📤 Пробрасывание исключений

Если метод не обрабатывает исключение, он обязан указать это:

```java
public void readFile() throws IOException {
    // чтение файла
}
```


## 🚨 Генерация исключений вручную

```java
throw new IllegalArgumentException("Неверный аргумент");
```


## 👷‍♂️ Создание собственного исключения

```java
public class MyException extends Exception {
    public MyException(String message) {
        super(message);
    }
}
```

Или unchecked:

```java
public class MyRuntimeException extends RuntimeException {
    ...
}
```


## 🔁 Multicatch (Java 7+)

```java
try {
    ...
} catch (IOException | SQLException ex) {
    ...
}
```


## ⚙️ `try-with-resources` (Java 7+)

Автоматическое закрытие ресурсов (`AutoCloseable`):

```java
try (BufferedReader br = new BufferedReader(new FileReader("file.txt"))) {
    ...
}
```


## 🧠 Частые ошибки

| Ошибка                                 | Почему плохо                   |
| -------------------------------------- | ------------------------------ |
| `catch (Exception e)` без логирования  | Глушит ошибку                  |
| Глубокая вложенность try-catch-finally | Ухудшает читаемость            |
| Игнорирование `InterruptedException`   | Мешает корректной отмене задач |
| Перехват `Throwable`                   | Ловит даже `Error`, что опасно |


## 🔎 Best Practices

✅ Перехватывай только то, что **можешь обработать**
✅ Логируй исключения: `logger.error("Ошибка", e)`
✅ Используй `try-with-resources`
✅ Не лови `Exception` или `Throwable`, если не уверен
✅ Создавай meaningful кастомные исключения
✅ В `finally`**никогда не бросай исключения**, иначе потеряешь основные


## 📚 Часто используемые стандартные исключения

| Исключение                  | Когда возникает                       |
| --------------------------- | ------------------------------------- |
| `NullPointerException`      | Доступ к `null` ссылке                |
| `IllegalArgumentException`  | Метод получил некорректный аргумент   |
| `IllegalStateException`     | Метод вызван в неправильное время     |
| `IndexOutOfBoundsException` | Индекс вне диапазона (массив, список) |
| `ArithmeticException`       | Деление на 0                          |
| `IOException`               | Ошибка ввода-вывода                   |
| `FileNotFoundException`     | Файл не найден                        |
| `ClassCastException`        | Некорректное приведение типов         |
| `NumberFormatException`     | Ошибка парсинга чисел                 |


## 🧪 Checked vs Unchecked — сравнение

| Критерий                    | Checked (`IOException`)      | Unchecked (`NullPointerException`)      |
| --------------------------- | ---------------------------- | --------------------------------------- |
| Требуется обработка?        | ✅ Да                         | ❌ Нет                                   |
| Контролируется компилятором | ✅ Да                         | ❌ Нет                                   |
| Типичная причина            | Внешние ресурсы              | Ошибки программиста                     |
| Уместность                  | API, работа с сетью, файлами | Внутренние ошибки, лень программиста 😅 |

