
## 1. Что такое тип данных?

**Тип данных** — это характеристика переменной или выражения, которая определяет:

* **какие значения** могут храниться (например, числа, символы, объекты);
* **какие операции** допустимы (например, сложение для чисел, конкатенация для строк);
* **как хранится** в памяти (размер и представление);
* **как проверяется** во время компиляции и выполнения.

Java — **строго типизированный язык**: каждая переменная и выражение имеет определённый тип, и компилятор проверяет совместимость типов.


***

## 2. Две большие категории типов

В Java есть **две фундаментальные категории типов**:

### 🔸 Примитивные (primitive types)

* Хранят **значение напрямую**.
* Не являются объектами (за редким исключением в JDK internals).
* Работают быстрее, т.к. обходятся без heap allocation (часто на стеке или в регистрах CPU).
* Всего 8 типов.

### 🔸 Ссылочные (reference types)

* Хранят **ссылку на объект в heap**.
* Значение переменной = «адрес» объекта.
* Могут быть `null`.
* Это: классы, интерфейсы, массивы, перечисления, аннотации.


***

## 3. Примитивные типы (8)

| Категория    | Тип       | Размер (бит) | Значения                       | По умолчанию |
| ------------ | --------- | ------------ | ------------------------------ | ------------ |
| Логический   | `boolean` | JVM-зависимо | `true` / `false`               | `false`      |
| Символьный   | `char`    | 16           | Unicode-символ (0–65535)       | `'\u0000'`   |
| Целые        | `byte`    | 8            | -128 … 127                     | `0`          |
|              | `short`   | 16           | -32,768 … 32,767               | `0`          |
|              | `int`     | 32           | -2,147,483,648 … 2,147,483,647 | `0`          |
|              | `long`    | 64           | от -2^63 до 2^63-1             | `0L`         |
| Вещественные | `float`   | 32           | IEEE 754, одинарная точность   | `0.0f`       |
|              | `double`  | 64           | IEEE 754, двойная точность     | `0.0d`       |

📌 Особенности:

* `boolean` в байткоде может храниться как `int` (0/1).
* `char` в Java — **Unicode 16-bit**, а не ASCII (поддерживает любой язык).
* `float` и `double` подвержены проблемам точности.


***

## 4. Ссылочные типы

### 4.1 Классы

* Определяются с помощью `class`.
* Экземпляры создаются через `new`.
* Хранятся в heap.
* Переменная хранит ссылку, а не сам объект.

### 4.2 Интерфейсы

* Определяют контракт (методы без реализации или с default-реализацией).
* Тип переменной может быть интерфейсом.

### 4.3 Массивы

* Всегда объект (даже массив примитивов).
* Внутри содержит непрерывный блок памяти.
* Тип: `int[]`, `String[]`.

### 4.4 Перечисления (enum)

* Особый вид классов, ограниченный фиксированным набором значений.

### 4.5 Аннотации

* Метаданные, которые тоже являются типами (`@interface`).


***

## 5. Отличия примитивов и ссылочных типов

| **Характеристика** | **Примитивы**               | **Ссылочные**          |
| ------------------ | --------------------------- | ---------------------- |
| Где хранятся       | В стеке (или регистрах CPU) | В heap, ссылка в стеке |
| Null               | Нет (`0` — это число)       | Может быть `null`      |
| Размер             | Фиксированный               | Может быть переменным  |
| ООП                | Не объекты                  | Всегда объекты         |
| Автобоксинг        | Можно обернуть (`Integer`)  | Не нужно               |


***

## 6. Обёрточные классы (Wrapper types)

Для каждого примитива есть **класс-обёртка** в пакете `java.lang`:

* `byte → Byte`
* `short → Short`
* `int → Integer`
* `long → Long`
* `float → Float`
* `double → Double`
* `char → Character`
* `boolean → Boolean`

Особенности:

* Нужны для работы с коллекциями (`List<Integer>` вместо `List<int>`).
* Поддерживают **автобоксинг** (автоматическое преобразование примитива в объект) и **распаковку**:

```java
int x = 5;
Integer y = x; // автобоксинг
int z = y;     // анбоксинг
```

* Обёртки **immutable** (значение изменить нельзя).
* Имеют константы (`Integer.MAX_VALUE`) и утилиты (`Integer.parseInt`).


***

## 7. Таблица: Примитивы vs Обёртки

| **Примитив** | **Обёртка** | **Значение по умолчанию** | **Может быть null?** |
| ------------ | ----------- | ------------------------- | -------------------- |
| int          | Integer     | 0                         | Да                   |
| boolean      | Boolean     | false                     | Да                   |
| double       | Double      | 0.0                       | Да                   |


***

## 8. var (с Java 10)

* `var` — это **синтаксический сахар** для локальных переменных.
* Тип всё равно определяется **на этапе компиляции**.
* Это **не динамическая типизация** (в отличие от Python, JS).
  Пример:

```java
var x = 10;        // int
var s = "Hello";   // String
```


***

## 9. Особенность Java: строгая проверка типов

* Преобразования выполняются только **явно** или по правилам widening/narrowing.
* Например:

```java
int x = 10;
long y = x;      // widening (автоматически)
int z = (int) y; // narrowing (нужно приведение)
```

