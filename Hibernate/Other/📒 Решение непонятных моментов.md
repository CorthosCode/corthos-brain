# 📒 Решение непонятных моментов

## Почему без ORM нужно вручную конвертировать **`ResultSet → объекты`**?



Когда мы работаем **чистым JDBC**, мы пишем SQL-запрос и получаем результат в виде **ResultSet** (по сути — таблица строк и колонок).

👉 JDBC не знает, что у тебя есть класс `User`. Он работает только с сырыми данными (`String`, `int`, `Date` и т.д.).

Поэтому тебе нужно руками читать каждую колонку и собирать объект.

Пример без ORM (JDBC):

```java
String sql = "SELECT id, name FROM users";
Statement stmt = conn.createStatement();
ResultSet rs = stmt.executeQuery(sql);

List<User> users = new ArrayList<>();
while (rs.next()) {
    User user = new User();
    user.setId(rs.getLong("id"));
    user.setName(rs.getString("name"));
    users.add(user);
}
```

С ORM (Hibernate):

```java
List<User> users = session.createQuery("FROM User", User.class).list();
```

👉 Разница: ORM сам знает, как преобразовать строки таблицы в объекты.

Без ORM — ты пишешь это вручную каждый раз.





***

## SQL-диалект — зачем нужны разные?



**JDBC-драйвер (****`driver_class`****)**

* Это просто «транспортный слой» между Java и конкретной БД.
* Он умеет подключаться и выполнять SQL.
* **НО**: драйвер не знает, какой SQL нужно генерировать — он просто принимает строки.

Пример:

```java
Connection conn = DriverManager.getConnection("jdbc:postgresql://localhost/db", "user", "pass");
Statement stmt = conn.createStatement();
stmt.execute("select * from users limit 10");
```

👉 JDBC-драйвер тупо передаст строку в PostgreSQL.



**Диалект (****`hibernate.dialect`****)**

* Hibernate сам генерирует SQL (например, `INSERT`, `UPDATE`, `JOIN`, `LIMIT`).
* Чтобы правильно его сгенерировать, Hibernate должен знать, какие синтаксические особенности есть у твоей БД.

👉 Здесь и нужен **dialect**: он подсказывает Hibernate, как именно формировать SQL.



**SQL** — это стандарт, но каждая БД (PostgreSQL, MySQL, Oracle, MSSQL) имеет свои особенности:

* типы данных (`AUTO_INCREMENT` в MySQL, `SERIAL`/`IDENTITY` в PostgreSQL).
* функции (`NOW()` в MySQL vs `CURRENT_TIMESTAMP` в Oracle).
* ограничения (`LIMIT` в MySQL vs `FETCH FIRST` в Oracle).

Пример:

```xml
<property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
```

Hibernate сам подстроит SQL:

в PostgreSQL:

```sql
select * from users limit 10
```

в Oracle:

```sql
select * from users fetch first 10 rows only
```



**Бывают ли разные диалекты для одного PostgreSQL?**

У PostgreSQL бывают разные версии (9.x, 10, 11, 12, 13, 14, 15…). У каждой версии свои новые фичи и нюансы.

Hibernate поддерживает несколько диалектов:

* `PostgreSQLDialect` — базовый.
* `PostgreSQL95Dialect` — для PostgreSQL 9.5+.
* `PostgreSQL10Dialect` — для PostgreSQL 10+.
* `PostgreSQL14Dialect` — для PostgreSQL 14+.

📌 Пример: в новых версиях PostgreSQL появился `generated as identity` вместо `serial`.

Соответственно, разные диалекты знают, какие ключевые слова использовать.





***

## Почему `jakarta.persistence`, а не `hibernate`?



Исторически:

* Был стандарт **JPA (Java Persistence API)** → аннотации (`@Entity`, `@Id`, `@Table`) определены именно в JPA.
* Hibernate = одна из реализаций JPA.

👉 Поэтому мы используем:

* **Аннотации JPA** из пакета `jakarta.persistence.*`.
* **Реализацию Hibernate** (`org.hibernate.Session`, `SessionFactory`).

Пример:

```java
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import org.hibernate.Session;
```

Так Hibernate остаётся совместимым с JPA: если захочешь перейти на EclipseLink вместо Hibernate, аннотации менять не придётся.





***

## Разница в конфигурации: `addAnnotatedClass` или `configure()`

Есть два варианта сказать Hibernate, какие классы являются сущностями:



**Вариант 1 — через XML (****`hibernate.cfg.xml`****):**

```xml
<mapping class="com.example.User"/>
```

И код:

```java
new Configuration().configure().buildSessionFactory();
```

Здесь Hibernate сам найдёт классы из XML.



**Вариант 2 — через Java:**

```java
new Configuration()
    .configure("hibernate.cfg.xml")
    .addAnnotatedClass(User.class)
    .buildSessionFactory();
```

👉 Разница:

* `.configure()` — читает только XML.
* `.addAnnotatedClass(User.class)` — явно добавляет класс (полезно, если в XML не описано).

В Spring Boot чаще всего конфигурация идёт через сканирование пакетов, и `addAnnotatedClass` не нужен.





***

## Что такое Flush?

**Flush = синхронизация persistence context с БД**.

Persistence context (сессия) хранит в памяти список **managed объектов** и их изменения.

Hibernate откладывает SQL до тех пор, пока не нужно.

Пример:

```java
Session session = factory.openSession();
Transaction tx = session.beginTransaction();

User u = new User("Alex");
session.save(u); // Hibernate ещё может не выполнить INSERT

u.setName("Alexander"); // Hibernate видит dirty checking

session.flush(); // вот тут Hibernate гарантированно выполнит INSERT и UPDATE

tx.commit();
```

👉 `commit()` всегда вызывает `flush()`.

Но `flush()` можно вызвать вручную (например, если тебе нужно, чтобы SQL выполнился прямо сейчас).





***

## Как работает `clear()`? Что за контекст?

**Persistence Context** (он же **1-й уровень кеша**) = «коробка» внутри сессии, где Hibernate хранит все объекты, которые он загрузил или сохранил.

Пример:

```java
Session session = factory.openSession();
User u1 = session.get(User.class, 1L); // загрузка из БД
User u2 = session.get(User.class, 1L); // возьмёт из контекста, без SQL!
```

👉 `session.clear()` очищает этот контекст. Все объекты внутри становятся **detached**.

Пример:

```java
Session session = factory.openSession();
Transaction tx = session.beginTransaction();

User u = session.get(User.class, 1L); // Persistent
System.out.println(session.contains(u)); // true

session.clear(); // очищаем контекст
System.out.println(session.contains(u)); // false (u теперь Detached)

tx.commit();
```

⚠️ Важно: `clear()` **не удаляет объекты из БД**, а просто «забывает» про них внутри текущей сессии.





***

## Почему `@Entity` и `@Table` разделены?

* `@Entity` говорит: **этот класс управляется JPA/Hibernate**, он маппится на таблицу.
* `@Table` — даёт **дополнительные настройки таблицы**:
  * имя (`name`)
  * схема (`schema`)
  * уникальные ограничения (`uniqueConstraints`)
  * индексы (`indexes`)

Пример:

```java
@Entity
@Table(
    name = "users",
    schema = "public",
    uniqueConstraints = {@UniqueConstraint(columnNames = {"email"})},
    indexes = {@Index(name = "idx_users_name", columnList = "name")}
)
public class User {
    @Id
    private Long id;
    private String name;
    private String email;
}
```

👉 Если не написать `@Table`, Hibernate сам создаст таблицу с именем класса (`User` → `user`).

Но у тебя не будет возможности задать индексы, уникальные ключи и т.д.





***

## uniqueConstraints = {@UniqueConstraint(columnNames = {"email"})}

Это значит: Hibernate создаст в БД **уникальное ограничение** на колонку `email`.

Пример таблицы:

```sql
create table users (
    id bigint not null,
    name varchar(255),
    email varchar(255),
    primary key (id),
    unique (email)  -- запрещает дублирующиеся email
);
```

Эффект:

* можно вставить `("vasya@mail.com")` один раз,
* но при попытке вставить ещё одного юзера с тем же email — будет ошибка `Unique constraint violation`.

👉 Это **на уровне БД**, в отличие от валидации в коде (`@Column(unique = true)` тоже делает уникальное ограничение, но `@Table(uniqueConstraints=...)` позволяет задать составные ключи).

Например:

```sql
@Table(
    uniqueConstraints = @UniqueConstraint(columnNames = {"first_name", "last_name"})
)
```

Здесь уникальность проверяется **по комбинации полей** (Имя+Фамилия).













