
## 🧩 Что это такое

**Base64** — это способ кодирования бинарных данных (файлы, байты, картинки) в текстовый формат, состоящий только из ASCII-символов. Он нужен, чтобы можно было безопасно передавать данные через каналы, где поддерживается только текст (например, HTTP-заголовки, JSON, XML, e-mail).


***

## ⚙️ Принцип работы

* Берётся 3 байта (3×8 = 24 бита).
* Эти 24 бита разбиваются на 4 группы по 6 бит.
* Каждая группа (6 бит) кодируется символом из таблицы Base64 (64 символа = A–Z, a–z, 0–9, +, /).
* Если данных не хватает до кратности 3 байтам, добавляются `=` в конце для выравнивания.

Пример:

```java
"Man" → "TWFu"
```

(“M” = 77, “a” = 97, “n” = 110 → \[01001101 01100001 01101110] → \[010011 010110 000101 101110] → T W F u)


***

## 💻 В Java

Кодирование:

```java
import java.util.Base64;

public class Base64Example {
    public static void main(String[] args) {
        String original = "Hello, Security!";
        String encoded = Base64.getEncoder().encodeToString(original.getBytes());
        System.out.println("Encoded: " + encoded);
    }
}
```

```java
Encoded: SGVsbG8sIFNlY3VyaXR5IQ==
```


Декодирование:

```java
import java.util.Base64;

public class Base64Decode {
    public static void main(String[] args) {
        String encoded = "SGVsbG8sIFNlY3VyaXR5IQ==";
        byte[] decodedBytes = Base64.getDecoder().decode(encoded);
        String decoded = new String(decodedBytes);
        System.out.println("Decoded: " + decoded);
    }
}
```

```java
Decoded: Hello, Security!
```


***

## 🔒 Важно помнить

* **Base64 не шифрует**, а только **кодирует**. Это *не защита*, а способ безопасно передать данные в текстовом виде.
* Для шифрования нужно использовать криптографию (например, AES, RSA и т. д.).
* В Java есть разные варианты кодеров:
  * `Base64.getEncoder()` — обычный.
  * `Base64.getUrlEncoder()` — безопасен для URL (`+` и `/` заменены на `-` и `_`).
  * `Base64.getMimeEncoder()` — форматирует с переносами строк (например, для e-mail).

