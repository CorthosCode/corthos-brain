
**Keycloak** — это *Identity and Access Management (IAM)* система с открытым исходным кодом.

Она:
* хранит пользователей, их роли и права,
* управляет логином, регистрацией, SSO и токенами,
* поддерживает протоколы **OAuth 2.0**, **OpenID Connect (OIDC)** и **SAML 2.0**,
* легко интегрируется с Java/Spring-приложениями.

👉 Проще говоря — **вынесенная аутентификация**, где твое приложение больше не хранит пользователей и пароли.


[[🔐 1. Идентификация, авторизация, аутентификация]]


***

## 🔐 1. Аутентификация (Authentication)

Аутентификация — это процесс **проверки личности пользователя** («Кто вы?»).

### 🔹 Поддерживаемые протоколы аутентификации

| **Протокол/Стандарт**       | **Описание**                                                                                                                                                                                      |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **OpenID Connect (OIDC)**   | Современный стандарт на базе OAuth 2.0. Используется для SSO (Single Sign-On). Возвращает ID Token (JWT) с информацией о пользователе.**Наиболее рекомендуемый для веб- и мобильных приложений**. |
| **OAuth 2.0**               | Стандарт авторизации, но часто используется и для аутентификации (в связке с OIDC). Keycloak выдаёт Access Token для доступа к API.                                                               |
| **SAML 2.0**                | Старый, но надёжный стандарт, часто используется в корпоративной среде (например, интеграция с Active Directory, SAP, Salesforce). Поддерживает SSO между доменами.                               |
| **LDAP / Active Directory** | Keycloak может**делегировать аутентификацию**внешним LDAP/AD-серверам. Пользователи логинятся через корпоративные учётные записи.                                                                 |
| **Kerberos**                | Для интеграции с Windows-инфраструктурой (редко используется в вебе).                                                                                                                             |


### 🔹 Способы входа (User Authentication Methods)

Keycloak поддерживает **гибкие методы входа**, которые можно комбинировать в **flows**:

| **Метод**                      | **Описание**                                                                                                                  |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| **Пароль (Username/Password)** | Стандартный логин.                                                                                                            |
| **Временные пароли (OTP)**     | Одноразовые пароли через **TOTP**(Google Authenticator, Authy) или **HOTP**. Поддержка **MFA (Multi-Factor Authentication)**. |
| **WebAuthn / FIDO2**           | Аутентификация через биометрию (Touch ID, Face ID) или физические ключи (YubiKey). Безопасно и удобно.                        |
| **Social Login**               | Вход через Google, GitHub, Facebook, LinkedIn и др. (через Identity Providers).                                               |
| **Identity Brokering**         | Единый вход через внешние IdP (например, Azure AD, Auth0, другой Keycloak).                                                   |
| **Client Secret / JWT**        | Аутентификация                                                                                                                |

> ⚙️ Всё это настраивается в админке Keycloak через **Authentication Flows** (например: пароль + TOTP = MFA).


***

## 🔑 2. Авторизация (Authorization)

Авторизация — это **проверка прав доступа** («Что вы можете делать?»).

Keycloak поддерживает **два основных подхода**:


### 🔸 A. Role-Based Access Control (RBAC) — на основе ролей

- Пользователям или группам назначаются **роли** (например, `admin`, `user`, `editor`).
- Приложения проверяют наличие ролей в токене (в `realm roles` или `client roles`).
- Пример проверки в коде:

```java
if (token.hasRealmRole("admin")) { ... }
```


> ✅ Просто, понятно, подходит для большинства приложений.


### 🔸 B. User-Managed Access (UMA) 2.0 / Fine-Grained Authorization

- **Гибкая, атрибутивная авторизация** (ABAC-like).
- Позволяет защищать **конкретные ресурсы** (например, «документ 123») и управлять доступом к ним динамически.
- Используется для **Resource Server** (например, REST API), где права зависят от:
    - Кто запрашивает?
    - Что запрашивает?
    - В каком контексте?

> ✅ Подходит для сложных сценариев: медицинские данные, совместный доступ, enterprise-приложения.

Пример:

> Пользователь A даёт пользователю B право **редактировать** свой документ X.


### 🔸 C. Policy Enforcers (в Spring Boot и др.)

Keycloak предоставляет **адаптеры** для популярных фреймворков:

- Spring Security
- Node.js
- Quarkus
- .NET

Они могут **автоматически проверять** роли или политики на основе токена.


***

## 🌐 3. Интеграции и расширяемость

| **Возможности**           | **Описание**                                                                 |
| ------------------------- | ---------------------------------------------------------------------------- |
| **Custom Authenticators** | Можно написать свой Java-провайдер для аутентификации (например, через SMS). |
| **Event Listeners**       | Логировать входы, выходы, ошибки.                                            |
| **Themes**                | Кастомизировать страницы логина/регистрации.                                 |
| **User Storage SPI**      | Интеграция с кастомной БД пользователей (не только LDAP или внутренняя БД).  |
| **Brute Force Detection** | Блокировка после N неудачных попыток.                                        |


***

## 📦 Архитектурные сущности в Keycloak

- **Realm** — изолированное пространство (например, `my-app-realm`).
- **Client** — ваше приложение (фронтенд, бэкенд, мобильное).
- **User** — конечный пользователь.
- **Identity Provider (IdP)** — внешний провайдер (Google, Azure AD).
- **Role / Group / Policy** — для авторизации.


---

## Полезные ключи при работе с Keycloak в контейнере

| **Переменная окружения**      | **Описание**                                                                              |
| ----------------------------- | ----------------------------------------------------------------------------------------- |
| KC_HEALTH_ENABLED             | Включает механизм проверки работоспособности (healthcheck). Значение: `true` или `false`. |
| KEYCLOAK_ADMIN                | Имя пользователя администратора Keycloak (например,admin).                                |
| KEYCLOAK_ADMIN_PASSWORD       | Пароль администратора Keycloak.                                                           |
| KC_DB_URL                     | URL для подключения к внешней базе данных.                                                |
| KC_DB_USERNAME                | Имя пользователя базы данных.                                                             |
| KC_DB_PASSWORD                | Пароль для базы данных.                                                                   |
| KC_HTTP_PORT                  | Порт для HTTP API Keycloak (по умолчанию 8080).                                           |
| KC_HOSTNAME_STRICT            | Отключает строгую проверку hostname (например, `false`).                                  |
| KC_METRICS_ENABLED            | Включает экспорт метрик для мониторинга (например, для Prometheus). Значение: `true`.     |
| KC_LOG_LEVEL                  | Уровень логирования (например, DEBUG, INFO, WARN).                                        |
| JAVA_OPTS                     | Дополнительные JVM опции (например, для памяти или отладки).                              |
| KC_CACHE_STACK                | Тип кеша, например redis для внешнего кэша.                                               |
| KC_METRICS_PROMETHEUS_ENABLED | Включает Prometheus метрики (значение `true`).                                            |

