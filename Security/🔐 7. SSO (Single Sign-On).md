
## 🧩 Что такое SSO

**SSO (Single Sign-On)** — это механизм, позволяющий пользователю **один раз пройти аутентификацию**, чтобы потом автоматически входить во все связанные приложения и сервисы **без повторного ввода логина и пароля**.

**Пример:**
Ты залогинился в Google — и без повторного входа можешь открыть Gmail, YouTube, Drive и т.д.


***

## ⚙️ Как это работает в общем виде

1. **Пользователь** заходит в приложение (Client App).
2. Приложение видит, что пользователь **ещё не аутентифицирован**, и **перенаправляет** его на **центральный сервер авторизации (Identity Provider, IdP)**.
3. Пользователь проходит аутентификацию (вводит логин/пароль).
4. IdP **создаёт токен** (обычно JWT или SAML-ассершн) и **возвращает** его приложению.
5. Приложение **доверяет этому токену** (оно знает публичный ключ IdP) и пускает пользователя.
6. При входе в другое приложение — оно тоже перенаправит на IdP, но IdP уже знает, что пользователь вошёл, и просто выдаст токен без повторного логина.


***

## 🔑 Основные участники

| Компонент                     | Роль                                                                      |
| ----------------------------- | ------------------------------------------------------------------------- |
| **User**                      | человек, который логинится                                                |
| **Client (Service Provider)** | приложение, в которое пользователь заходит                                |
| **Identity Provider (IdP)**   | центральный сервер аутентификации (Keycloak, Auth0, Okta, Azure AD и др.) |


***

## 🧰 Стандарты, на которых основано SSO

| Протокол                              | Где используется                         | Особенности                                             |
| ------------------------------------- | ---------------------------------------- | ------------------------------------------------------- |
| **SAML 2.0**                          | Корпоративные системы, старые интеграции | XML, обмен через браузер, часто в enterprise            |
| **OAuth 2.0 + OpenID Connect (OIDC)** | Современные веб/мобайл приложения        | Основан на JWT, JSON, REST, поддерживает refresh токены |
| **Kerberos**                          | Windows-домены, внутренние сети          | Сетевой протокол, используется Active Directory         |


***

## 🧠 Пример схемы (OIDC / JWT)

```java
[User] → запрос /login → [App]
  ↳ redirect → [Auth Server / IdP]
       ↳ проверка логина/пароля
       ↳ выдача JWT Access Token + ID Token
  ↳ redirect обратно → [App]
       ↳ App сохраняет токен → доступ к API
```

