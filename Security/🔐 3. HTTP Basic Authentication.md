
## Что такое Basic Auth

**Basic Auth** — это самый простой способ аутентификации в HTTP-протоколе. Она основана на том, что клиент отправляет **логин и пароль** в **заголовке Authorization** каждого запроса.


***

## 📦 Формат заголовка

```http
Authorization: Basic <base64(username:password)>
```

Например:

```http
username = admin
password = 1234
```

Сначала объединяем:

```http
admin:1234
```

Потом кодируем в Base64:

```http
YWRtaW46MTIzNA==
```

И получаем заголовок:

```http
Authorization: Basic YWRtaW46MTIzNA==
```

📌 Важно: **Base64 не шифрует!**

Данные легко декодируются обратно. Поэтому Basic Auth **должен использоваться только через HTTPS**.


***

## 💻 Пример на чистой Java

```java
import java.net.HttpURLConnection;
import java.net.URL;
import java.util.Base64;

public class BasicAuthExample {
    public static void main(String[] args) throws Exception {
        String user = "admin";
        String password = "1234";
        String auth = user + ":" + password;
        String encodedAuth = Base64.getEncoder().encodeToString(auth.getBytes());

        URL url = new URL("https://example.com/api/secure");
        HttpURLConnection conn = (HttpURLConnection) url.openConnection();
        conn.setRequestProperty("Authorization", "Basic " + encodedAuth);

        System.out.println("Response code: " + conn.getResponseCode());
    }
}
```


***

## 🧩 Пример в Spring Boot (Server side)

Spring Security умеет Basic Auth "из коробки" — достаточно одной конфигурации.

✅ Spring Boot 3+ (SecurityFilterChain)

```java
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
public class SecurityConfig {

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/public/**").permitAll()  // без авторизации
                .anyRequest().authenticated()              // всё остальное — с авторизацией
            )
            .httpBasic(Customizer.withDefaults())   // включаем Basic Auth
            .and()
            .csrf().disable(); // отключаем CSRF для простоты
        return http.build();
    }
}
```


***

## ✅ Настройка пользователей

Можно задать пользователя прямо в `application.properties`:

```
spring.security.user.name=admin
spring.security.user.password=1234
```

Теперь любой запрос к `/` без авторизации получит `401 Unauthorized`.

Или через Java-конфигурацию

```java
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.core.userdetails.User;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.provisioning.InMemoryUserDetailsManager;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
public class UserConfig {

    @Bean
    public UserDetailsService userDetailsService(PasswordEncoder passwordEncoder) {
        var user = User.withUsername("admin")
                .password(passwordEncoder.encode("1234"))
                .roles("ADMIN")
                .build();

        return new InMemoryUserDetailsManager(user);
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```


***

## ⚠️ Когда использовать Basic Auth

✅ Подходит:
* для простых REST API, если используется HTTPS;
* для временных или внутренних сервисов;
* для автоматизации (например, Jenkins, monitoring, CI/CD).


🚫 Не подходит:
* для публичных приложений;
* если нет HTTPS;
* если нужно управлять сессиями, ролями, токенами и сроками действия.

