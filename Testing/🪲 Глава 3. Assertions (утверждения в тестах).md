# 🪲 Глава 3. Assertions (утверждения в тестах)

## Что такое assertions?

* Это **проверки в тестах**, которые сравнивают **ожидаемый результат** с **фактическим**.
* Если условие не выполняется → тест **падает**.
* Assertions делают тест **самодокументируемым**: сразу видно, что именно проверяется.

В JUnit они находятся в классе `org.junit.jupiter.api.Assertions`.





***

## Базовые assertions

```java
import static org.junit.jupiter.api.Assertions.*;
```



**`assertEquals(expected, actual)`****&#x20;— проверяет равенство значений.**

```java
assertEquals(5, calc.add(2, 3));
```



**`assertNotEquals(unexpected, actual)`****&#x20;— проверяет, что значения разные.**

```java
assertNotEquals(6, calc.add(2, 3));
```



**`assertTrue(condition)`****&#x20;— проверяет, что условие истинно.**

```java
assertTrue(result > 0);
```



**`assertFalse(condition)`****&#x20;— проверяет, что условие ложно.**

```java
assertFalse(result < 0);
```



**`assertNull(obj)`****&#x20;/&#x20;****`assertNotNull(obj)`****&#x20;— проверяет&#x20;****`null`****/не&#x20;****`null`****.**

```java
assertNotNull(user.getId());
```



**`assertSame(expected, actual)`****&#x20;/&#x20;****`assertNotSame(expected, actual)`****&#x20;— проверяет, что это один и тот же объект (по ссылке).**

```java
assertSame(user1, user2);
```





***

## Проверка исключений

Если метод должен бросить исключение, то используем `assertThrows`.

```java
@Test
void testDivisionByZero() {
    Calculator calc = new Calculator();
    assertThrows(ArithmeticException.class, () -> calc.divide(10, 0));
}
```





***

## Группировка проверок

Иногда нужно проверить несколько условий за раз:

* `assertAll(...)` позволяет объединять несколько проверок.
* Если одна упадёт — остальные всё равно выполнятся (полезно для DTO, моделей).

```java
@Test
void testUserFields() {
    User user = new User("Alice", 25);

    assertAll(
        () -> assertEquals("Alice", user.getName()),
        () -> assertEquals(25, user.getAge()),
        () -> assertNotNull(user.getName())
    );
}
```





***

## Кастомные сообщения ошибок

Для удобства отладки можно добавлять сообщение:

```java
assertEquals(5, calc.add(2, 3), "Сложение работает неправильно");
```

👉 Если тест упадёт, в отчёте будет сообщение:

```java
org.opentest4j.AssertionFailedError: Сложение работает неправильно
Expected :5
Actual   :4
```





***

## Современные библиотеки для утверждений

Хотя JUnit assertions достаточно, часто используют **расширенные библиотеки**:

* **AssertJ** — более читаемые проверки:

```java
assertThat(calc.add(2, 3)).isEqualTo(5);
assertThat(user.getName()).startsWith("A").endsWith("e");
```

* **Hamcrest** — матчеры:

```java
assertThat(result, is(5));
assertThat(name, startsWith("Al"));
```

Но на первых порах достаточно **стандартных JUnit assertions**.



