# 🪲 @MockBean vs Mockito.mock()

## Что делает `Mockito.mock()`

`Mockito.mock()` просто создаёт **локальный объект-мок** в тесте. Spring о нём ничего не знает.

Это просто обычная переменная в твоём тестовом классе.

```java
class UserServiceTest {

    private final UserRepository repo = Mockito.mock(UserRepository.class);
    private final UserService service = new UserService(repo);

    @Test
    void testWithMockito() {
        Mockito.when(repo.findById("1"))
               .thenReturn(new User("1", "Alice"));

        assertEquals("Alice", service.findUser("1").getName());
    }
}
```

✅ Подходит для:

* **чистых unit-тестов** (без Spring);
* когда ты сам вручную создаёшь объект, который тестируешь (`new UserService(...)`);
* когда хочешь, чтобы тесты были **максимально быстрыми** (без загрузки контекста).

❌ Не подходит для:

* тестов с `@SpringBootTest`, `@WebMvcTest` и другими Spring-аннотациями —
  &#x20;потому что **Spring не внедрит твой мок автоматически** в свои бины.





***

## Что делает `@MockBean`

`@MockBean` — это **Spring-aware мок**. Когда ты используешь его в тесте, Spring **встраивает его в контекст** — то есть заменяет настоящий бин на мок.

Пример:

```java
@SpringBootTest
class UserServiceSpringTest {

    @MockBean
    private UserRepository repo; // мок внутри Spring

    @Autowired
    private UserService service; // получает этот мок через DI

    @Test
    void testWithSpringMock() {
        Mockito.when(repo.findById("1"))
               .thenReturn(Optional.of(new User("1", "Alice")));

        assertEquals("Alice", service.findUser("1").getName());
    }
}
```

✅ Подходит для:

* **интеграционных тестов**, где тестируем класс, получаемый через Spring (`@Autowired`);
* случаев, когда тебе нужно **частично изолировать зависимость** (например, замокать только репозиторий, но оставить настоящий сервис);
* **`@SpringBootTest`**, **`@WebMvcTest`**, **`@DataJpaTest`** и т.д.

❌ Не подходит для:

* unit-тестов без контекста (он просто не сработает, Spring не поднимется).





***

## Сравнение по пунктам

| Критерий                                                    | `Mockito.mock()` | `@MockBean`                              |
| ----------------------------------------------------------- | ---------------- | ---------------------------------------- |
| Загрузка контекста                                          | ❌ Нет            | ✅ Да                                     |
| Скорость                                                    | ⚡ Очень быстрая  | 🐢 Медленнее (Spring поднимает контекст) |
| Автовнедрение (`@Autowired`)                                | ❌ Не работает    | ✅ Работает                               |
| Тип тестов                                                  | Unit-тесты       | Spring Boot / интеграционные             |
| Подмена зависимостей в контексте                            | ❌ Нет            | ✅ Да                                     |
| Использование с `MockMvc`, `@WebMvcTest`, `@SpringBootTest` | ❌ Нет            | ✅ Да                                     |



