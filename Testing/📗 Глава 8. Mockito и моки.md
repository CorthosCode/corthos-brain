
## Что такое моки?

Когда мы тестируем класс, он обычно **зависит от других объектов**: репозиториев, сервисов, API.

В unit-тестах мы не хотим работать с настоящей БД или сетью → нужно **подменить зависимости на фейковые**.

Виды тестовых двойников (test doubles):

* **Dummy** — заглушка, которая просто нужна, чтобы заполнить аргумент.
* **Stub** — объект с фиксированным поведением.
* **Mock** — объект, который можно «настроить» и проверить, вызывались ли у него методы.
* **Spy** — обёртка над реальным объектом, позволяющая отслеживать вызовы.

👉 Mockito работает в основном с **моками и спаями**.


***

## Подключение Mockito

**Maven:**

```xml
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-core</artifactId>
    <version>5.12.0</version>
    <scope>test</scope>
</dependency>
```

**Gradle:**

```groovy
testImplementation 'org.mockito:mockito-core:5.12.0'
```

Если используешь Spring Boot → часто подключают **`spring-boot-starter-test`**, и там Mockito уже есть.


***

## Первый пример — мок зависимостей

Допустим, у нас есть сервис:

```java
class UserService {
    private final UserRepository repository;

    UserService(UserRepository repository) {
        this.repository = repository;
    }

    public User findUser(String id) {
        return repository.findById(id);
    }
}

interface UserRepository {
    User findById(String id);
}

class User {
    private final String id;
    private final String name;
    User(String id, String name) { this.id = id; this.name = name; }
    public String getName() { return name; }
}
```

Тестируем `UserService`, но **не хотим настоящий&#x20;****`UserRepository`**.

Используем Mockito:

```java
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import static org.junit.jupiter.api.Assertions.*;

class UserServiceTest {

    @Test
    void shouldReturnUserFromRepository() {
        // создаём мок репозитория
        UserRepository repo = Mockito.mock(UserRepository.class);

        // настраиваем поведение: если вызван findById("1"), вернуть User("1", "Alice")
        Mockito.when(repo.findById("1")).thenReturn(new User("1", "Alice"));

        UserService service = new UserService(repo);

        User result = service.findUser("1");

        assertEquals("Alice", result.getName());
    }
}
```


***

## Проверка вызовов

Mockito позволяет не только настроить ответ, но и проверить, что метод **действительно вызывался**.

```java
@Test
void shouldCallRepository() {
    UserRepository repo = Mockito.mock(UserRepository.class);
    UserService service = new UserService(repo);

    service.findUser("1");

    Mockito.verify(repo).findById("1"); // проверяем, что метод был вызван
}
```


***

## Дополнительные возможности


**Проверка числа вызовов:**

```java
Mockito.verify(repo, Mockito.times(1)).findById("1");
Mockito.verify(repo, Mockito.never()).findById("2");
```


**Имитация исключений**:

```java
Mockito.when(repo.findById("404"))
       .thenThrow(new RuntimeException("User not found"));
```


**Аргумент-матчеры**:

```java
Mockito.when(repo.findById(Mockito.anyString()))
       .thenReturn(new User("X", "Test"));
```


***

## Аннотации Mockito

Чтобы писать тесты короче, можно использовать аннотации и `MockitoAnnotations`:

```java
import org.junit.jupiter.api.BeforeEach;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

class UserServiceTest {

    @Mock
    UserRepository repo;   // создаётся мок

    @InjectMocks
    UserService service;   // мок автоматически "внедряется" в UserService

    @BeforeEach
    void init() {
        MockitoAnnotations.openMocks(this);
    }

    @Test
    void shouldReturnUser() {
        Mockito.when(repo.findById("1"))
               .thenReturn(new User("1", "Alice"));

        assertEquals("Alice", service.findUser("1").getName());
    }
}
```


***

## Когда использовать Mockito

* Когда нужно протестировать бизнес-логику без настоящей БД/сети.
* Когда метод зависит от тяжёлых сервисов.
* Когда важно проверить **взаимодействие** между объектами (кто кого вызывает).

❌ Не стоит мокать всё подряд — это делает тесты хрупкими. Лучше использовать моки только для **внешних зависимостей**.

