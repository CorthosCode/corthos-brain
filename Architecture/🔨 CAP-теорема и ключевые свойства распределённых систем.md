

![](assets/1nkazzs_eew0vf3-IdHl4KRVmwPLo_RV60NSzWIlCLg=.png)


В распределённых системах (например, кластеры баз данных, распределённые файловые системы, микросервисы) есть три фундаментальных свойства:

1. **Consistency (Согласованность)** – все узлы видят одинаковые данные в одно и то же время.
2. **Availability (Доступность)** – каждый запрос к системе получает ответ (успешный или с ошибкой), система всегда "отзывается".
3. **Partition Tolerance (Устойчивость к разделению)** – система продолжает работать, даже если часть узлов потеряла связь друг с другом (сеть "разделилась").


***

## 📌 CAP-теорема (Э. Брювер) утверждает:
👉 В распределённой системе **невозможно одновременно обеспечить все три свойства**.
👉 Система может гарантировать **только два из трёх**.


***

## Доступность (Availability)

* Система считается доступной, если **каждый запрос получает ответ в разумное время**.
* Ответ не обязан содержать "актуальные данные" — главное, что система отвечает, а не "зависает".

👉 Пример:

* Если база данных доступна, она всегда отвечает на `SELECT` или `UPDATE`, даже если часть узлов работает с устаревшими данными.
* Если доступности нет — клиент может "ждать" бесконечно, пока система решает проблему.


***

## Устойчивость к разделению (Partition Tolerance)

* **Partition (разделение)** — это ситуация, когда сеть между узлами ломается, и часть узлов не может общаться с другими.
* **Partition Tolerance** — способность системы продолжать работу, даже если связь между узлами нарушена.

👉 Пример:

* У нас есть 3 сервера (Москва, Лондон, Нью-Йорк). Связь между Москвой и Лондоном пропала.
* Система с устойчивостью к разделению продолжит обслуживать клиентов и в Москве, и в Лондоне (пусть даже с разными версиями данных).
* Если системы нетерпимы к разделениям — всё "встанет", пока связь не восстановится.


***

## CAP: выбор между свойствами

Из трёх свойств **нельзя обеспечить все одновременно**.

* **CA (Consistency + Availability)**
  * Система всегда согласованная и доступная.
  * Но неустойчива к разделениям. Если сеть ломается — система "падает".
  * Пример: традиционные реляционные БД без репликации.
* **CP (Consistency + Partition tolerance)**
  * Система остаётся согласованной и устойчива к разделениям.
  * Но может терять доступность (часть клиентов ждёт или получает ошибку).
  * Пример: HBase, MongoDB в строгом режиме, PostgreSQL с репликацией и синхронными commit.
* **AP (Availability + Partition tolerance)**
  * Система доступна и устойчива к разделениям, но жертвует строгой согласованностью (возникает eventual consistency).
  * Пример: Cassandra, DynamoDB, Riak.


***

## Практические замечания

* В **реальном мире** большинство распределённых систем обязаны быть **Partition Tolerant** (сети ненадёжны, сбои неизбежны). Значит, разработчики чаще выбирают между **Availability (AP)** и **Consistency (CP)**.
* Есть ещё обобщения:
  * **PACELC-теорема** (расширение CAP): если есть разделение (P) → выбираем между A и C; если разделения нет (ELSE) → выбираем между Latency (задержкой) и Consistency.

