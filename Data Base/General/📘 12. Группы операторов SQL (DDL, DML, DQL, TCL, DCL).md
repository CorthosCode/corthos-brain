# 📘 Группы операторов SQL (DDL, DML, DQL, TCL, DCL)

![](assets/vmrKc4rhL1hQXHrHmbAPc1olafSH7_gj3Erk2O-nvPo=.png)





## 1. **DDL (Data Definition Language)** — язык определения данных

👉 Управляет структурой базы данных: таблицы, индексы, схемы.
&#x20;Операторы:

* `CREATE` — создание объектов (таблицы, схемы, базы, индекса и т. д.)
* `ALTER` — изменение структуры объекта
* `DROP` — удаление объектов
* `TRUNCATE` — очистка таблицы без фиксации транзакций (быстрая операция)
* `COMMENT` — комментарии к объектам
* `RENAME` — переименование объектов





***

## 2. **DML (Data Manipulation Language)** — язык манипуляции данными

👉 Вставка, изменение и удаление строк в таблицах.
&#x20;Операторы:

* `INSERT` — вставка данных
* `UPDATE` — обновление данных
* `DELETE` — удаление данных
* `MERGE` (или `UPSERT`) — вставка или обновление данных (зависит от СУБД)





***

## 3. **DQL (Data Query Language)** — язык запросов к данным

👉 Используется **только для выборки данных**, без изменения.
&#x20;Операторы:

* `SELECT` — выборка данных (с `WHERE`, `GROUP BY`, `HAVING`, `ORDER BY`, `JOIN`, `WINDOW FUNCTIONS` и т. д.)





***

## 4. **DCL (Data Control Language)** — язык управления доступом

👉 Управляет правами пользователей.
&#x20;Операторы:

* `GRANT` — выдача прав
* `REVOKE` — отзыв прав





***

## 5. **TCL (Transaction Control Language)** — язык управления транзакциями

👉 Управляет транзакциями в БД.
&#x20;Операторы:

* `COMMIT` — зафиксировать транзакцию
* `ROLLBACK` — откатить транзакцию
* `SAVEPOINT` — установить точку сохранения внутри транзакции
* `SET TRANSACTION` — задать параметры транзакции





***

## Итоговая структура

| Группа  | Назначение                      | Основные операторы                                         |
| ------- | ------------------------------- | ---------------------------------------------------------- |
| **DDL** | Определение структуры БД        | `CREATE`, `ALTER`, `DROP`, `TRUNCATE`, `COMMENT`, `RENAME` |
| **DML** | Манипуляция (изменение) данными | `INSERT`, `UPDATE`, `DELETE`, `MERGE`                      |
| **DQL** | Запрос (чтение) данных          | `SELECT`                                                   |
| **DCL** | Управление доступом             | `GRANT`, `REVOKE`                                          |
| **TCL** | Управление транзакциями         | `COMMIT`, `ROLLBACK`, `SAVEPOINT`, `SET TRANSACTION`       |



