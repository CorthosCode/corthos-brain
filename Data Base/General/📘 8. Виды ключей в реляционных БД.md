
![](assets/C-JIaFoA1mQmP8_vp6RQxUAOgFM6BFbS3WICEomPyCU=.png)


## Потенциальный ключ (Candidate Key)

* Это **любой набор атрибутов (полей)**, который может **однозначно идентифицировать кортеж (строку)** в таблице.
* У таблицы может быть несколько таких ключей.
* Из всех потенциальных выбирается **первичный**.

📌 Пример:

```sql
CREATE TABLE users (
    user_id SERIAL,
    email TEXT,
    passport_num TEXT,
    CONSTRAINT uq_email UNIQUE (email),
    CONSTRAINT uq_passport UNIQUE (passport_num)
);
```

👉 Здесь:

* `user_id`, `email`, `passport_num` — все могут быть потенциальными ключами.


***

## Первичный ключ (Primary Key)

* Один из потенциальных ключей, который **выбран системой как основной идентификатор**.
* Уникальный, **NOT NULL** автоматически.
* Может быть простым (1 колонка) или составным (несколько колонок).

📌 Пример:

```sql
CREATE TABLE employees (
    emp_id SERIAL PRIMARY KEY,
    email TEXT UNIQUE
);
```

👉 `emp_id` — первичный ключ,

👉 `email` — альтернативный (см. ниже).


***

## Альтернативный ключ (Alternate Key)

* Потенциальный ключ, **не выбранный** в качестве первичного.
* Обеспечивает уникальность, но не является "главным" идентификатором.

📌 В примере выше `email` — альтернативный ключ.


***

## Суррогатный ключ (Surrogate Key)

* Искусственно введённый атрибут, который **не несёт бизнес-смысла**, но используется как первичный ключ.
* Обычно — автоинкремент (`SERIAL`, `BIGSERIAL`) или UUID.

📌 Пример:

```sql
CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,   -- суррогатный
    order_number TEXT              -- бизнес-ключ (может повторяться)
);
```

👉 `order_id` — суррогатный ключ.


***

## Составной ключ (Composite Key)

* Первичный ключ, состоящий из **нескольких полей**.
* Используется, когда одно поле не может гарантировать уникальность.

📌 Пример:

```sql
CREATE TABLE course_enrollments (
    student_id INT,
    course_id INT,
    PRIMARY KEY (student_id, course_id)
);
```

👉 Каждое поле отдельно не уникально, но пара уникальна.


***

## Внешний ключ (Foreign Key)

* Поле (или набор полей), которое **ссылается на первичный/уникальный ключ** в другой таблице.
* Гарантирует **ссылочную целостность**.

📌 Пример:

```sql
CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,
    customer_id INT REFERENCES customers(customer_id) ON DELETE CASCADE
);
```

👉 `customer_id` в `orders` — внешний ключ на `customers`.

