# 📘 Реляционные операции и аналогии с множествами

## **Объединение (UNION) → аналог сложения множеств**

* Берёт строки из двух таблиц (отношений) и объединяет их.
* Условие: таблицы должны быть **совместимы по схеме** (одинаковое количество атрибутов и одинаковые домены).
* Результат = множество всех кортежей из обеих таблиц без дубликатов.

📌 Пример:

```make
A = {1, 2, 3}
B = {3, 4, 5}
A ∪ B = {1, 2, 3, 4, 5}
```

SQL:

```sql
SELECT name FROM Customers_US
UNION
SELECT name FROM Customers_EU;
```





***

## **Пересечение (INTERSECTION) → аналог умножения множеств**

* Возвращает только те строки, которые есть **и в одной, и в другой таблице**.

📌 Пример:

```make
A = {1, 2, 3}
B = {3, 4, 5}
A ∩ B = {3}
```

SQL:

```sql
SELECT name FROM Customers_US
INTERSECT
SELECT name FROM Customers_EU;
```





***

## **Разность (DIFFERENCE, EXCEPT/MINUS) → аналог вычитания множеств**

* Берёт строки, которые есть в одной таблице, но нет в другой.

📌 Пример:

```make
A = {1, 2, 3}
B = {3, 4, 5}
A − B = {1, 2}
```

SQL:

```sql
SELECT name FROM Customers_US
EXCEPT
SELECT name FROM Customers_EU;
```





***

## **Декартово произведение (CARTESIAN PRODUCT, ×) → аналог перемножения множеств**

* Каждую строку из одной таблицы соединяет с каждой строкой другой таблицы.
* Результат = все возможные комбинации.

📌 Пример:

```make
A = {1, 2}
B = {x, y}
A × B = {(1,x), (1,y), (2,x), (2,y)}
```

SQL:

```sql
SELECT * FROM Products, Suppliers;
-- или
SELECT * FROM Products CROSS JOIN Suppliers;
```





***

## **Соединение (JOIN) → отфильтрованное декартово произведение**

* Берём декартово произведение и накладываем условие на совпадение ключей.

📌 Пример:

```make
A = { (1, "Иван"), (2, "Петр") }
B = { (1, "Заказ1"), (2, "Заказ2"), (3, "Заказ3") }

A ⋈ B (по id) = { (1,"Иван","Заказ1"), (2,"Петр","Заказ2") }
```

SQL:

```sql
SELECT c.name, o.order_id
FROM Customers c
JOIN Orders o ON c.id = o.client_id;
```





***

## **Проекция (PROJECTION, π) → аналог операции выбора столбцов**

* Выбираем только нужные атрибуты (убираем остальные).
* Удаляются дубликаты (так как результат — множество).

📌 Пример:

```make
Сотрудники = { (Иван, 30, IT), (Петр, 25, HR), (Иван, 30, IT) }
π_имя,отдел(Сотрудники) = { (Иван, IT), (Петр, HR) }
```

SQL:

```sql
SELECT DISTINCT name, department FROM Employees;
```





***

## **Выборка (SELECTION, σ) → аналог фильтрации множества**

* Выбираем подмножество строк, удовлетворяющих условию.

📌 Пример:

```make
Сотрудники = { (Иван, 30, IT), (Петр, 25, HR) }
σ_age>26(Сотрудники) = { (Иван, 30, IT) }
```

SQL:

```sql
SELECT * FROM Employees WHERE age > 26;
```



