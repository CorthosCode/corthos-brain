# 📘 ACID

**Транзакция** — это **логическая единица работы** с БД, которая состоит из одной или нескольких операций SQL (INSERT, UPDATE, DELETE и т.д.) и должна выполняться **либо полностью, либо не выполняться вовсе**.

👉 Пример:

Перевод денег с карты А на карту Б:

1. Списать деньги с карты А.
2. Зачислить деньги на карту Б.

Если операция (1) выполнится, а (2) — нет → деньги "пропали". Поэтому обе операции должны выполняться как **единое целое**.



Основная характеристика транзакционности — это **ACID**:

1. Atomicity (Атомарность)
2. Consistency (Согласованность)
3. Isolation (Изолированность)
4. Durability (Надёжность/устойчивость)





***

## Atomicity (Атомарность)

* Транзакция выполняется **целиком или не выполняется вообще**.
* Если где-то ошибка → откат (**ROLLBACK**).

Пример: при переводе денег, если не удалось зачислить сумму на карту Б → списание с карты А тоже отменяется.





***

## Consistency (Согласованность)

* После завершения транзакции база остаётся в **корректном состоянии**, не нарушаются ограничения (constraints, правила).

Пример: нельзя провести транзакцию так, чтобы на счету оказалось отрицательное значение, если такое запрещено.





***

## Isolation (Изолированность)

* Одновременные транзакции **не мешают друг другу**, результат будет таким, как если бы они выполнялись **по очереди**.

Пример: два человека одновременно переводят деньги со счёта. Даже если транзакции выполняются параллельно, итоговый баланс будет правильным.





***

## Durability (Надёжность/устойчивость)

* После подтверждения (**COMMIT**) изменения **сохраняются навсегда**, даже при сбое системы.

Пример: если после перевода денег отключилось электричество, данные всё равно должны сохраниться.





***

## Жизненный цикл транзакции

* **BEGIN TRANSACTION / START** → начало транзакции.
* Выполнение SQL-операций (INSERT, UPDATE, DELETE).
* Если всё успешно → **COMMIT** (сохранение).
* Если ошибка → **ROLLBACK** (отмена всех действий).





***

## Пример транзакции

```sql
BEGIN TRANSACTION;

UPDATE accounts SET balance = balance - 1000 WHERE id = 1;
UPDATE accounts SET balance = balance + 1000 WHERE id = 2;

COMMIT;
```

Если второй `UPDATE` не выполнится → будет `ROLLBACK`.





***

## Уровни изоляции транзакций (SQL стандарт)

Изоляция нужна, чтобы избежать ошибок при параллельной работе:



**Dirty Read (грязное чтение)** → транзакция читает данные, которые могут быть откатаны.

**Non-repeatable Read (неповторяющееся чтение)** → один и тот же SELECT в рамках транзакции возвращает разные результаты.

**Phantom Read (фантомные чтения)** → при повторном SELECT появляются новые строки, которых раньше не было.



1. **Read Uncommitted** (чтение неподтверждённых данных)
   * Можно читать данные, которые ещё не зафиксированы (**грязное чтение**).
   * Самый быстрый, но небезопасный.
2. **Read Committed** (чтение только зафиксированных данных)
   * Можно читать только подтверждённые данные.
   * Защита от грязного чтения, но возможны "неповторяющиеся чтения".
3. **Repeatable Read** (повторяемое чтение)
   * Гарантирует, что при повторном запросе данные не изменятся.
   * Защита от грязного и неповторяющегося чтения, но возможны фантомные строки.
4. **Serializable** (полная сериализация)
   * Максимальная изоляция, транзакции выполняются как будто **по очереди**.
   * Самый надёжный, но самый медленный.

[📘 Уровни изоляции транзакций (Isolation Levels)](file:///workspace/6873b63c-7b27-4903-873e-656e6b87e61d/zsO6rXk190l_-O51Y6ba6)



