# 📘 Теорема Хита (Heath’s theorem)

В **реляционных БД** часто возникает ситуация:

Таблица слишком большая и содержит *избыточность*.

Хотим разложить её на две поменьше — так, чтобы **ничего не потерялось** и чтобы потом можно было восстановить таблицу через `JOIN`.

👉 Теорема Хита как раз объясняет, **когда такое разложение возможно без потерь**.





***

## Формальное определение

Пусть у нас есть отношение (таблица) **R(A, B, C)**.

Если существует **функциональная зависимость**:

```sql
A → B
```

то таблицу **R** можно без потерь разложить на две таблицы:

* `R1(A, B)`
* `R2(A, C)`

и потом восстановить исходное R через `JOIN`:

```sql
R = R1 ⋈ R2
```





***

## Объяснение простым языком

* Если `A` определяет `B`, то **нет смысла хранить B рядом с C в той же таблице**.
* Мы можем разделить таблицу на `(A, B)` и `(A, C)`, а потом соединить их обратно по `A`.
* При этом информация **не потеряется** и не создадутся "ложные строки".





***

## Пример



**До разбиения (одна таблица):**

```sql
Student | Group | Curator
--------|-------|---------
Иван    | 101   | Петров
Анна    | 101   | Петров
Олег    | 102   | Сидоров
```

Здесь видно зависимость:

```sql
Group → Curator
```

(каждая группа имеет ровно одного куратора).



**После разбиения:**

`R1(Student, Group)`

```sql
Иван   | 101
Анна   | 101
Олег   | 102
```

`R2(Group, Curator)`

```sql
101 | Петров
102 | Сидоров
```

Восстановление:

```sql
SELECT Student, R1.Group, Curator
FROM R1
JOIN R2 USING(Group);
```

👉 Получим ту же таблицу, что и исходная.





***

## Когда полезна

* При **нормализации БД** (обычно при переходе к 3NF и BCNF).
* Чтобы убрать **избыточность** и **аномалии обновления**.
* Чтобы проверить, можно ли разбить таблицу **без потери информации**.



