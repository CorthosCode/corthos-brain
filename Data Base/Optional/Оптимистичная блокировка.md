# Оптимистичная блокировка

###### 🔹 Основные сведения

Оптимистичная блокировка (Optimistic Locking) — это стратегия работы с \<u>параллельными транзакциями в БД\</u>, которая предполагает, что конфликты при изменении данных случаются редко, но их нужно корректно обрабатывать.



Название \<u>«оптимистическая блокировка»\</u> (Optimistic Locking) происходит из философии её работы: она \<u>оптимистично\</u> предполагает, что \<u>конфликты\</u> при изменении данных случаются \<u>редко\</u>, и не блокирует ресурсы заранее. Вместо этого она позволяет всем работать параллельно, а конфликты разрешает только в момент \<u>сохранения данных\</u> — если они действительно возникли.





###### 🔹 Как это устроено в СУБД?

Допустим, есть таблица `users`:

| `id` | `name` | `balance` | `version` |
| ---- | ------ | --------- | --------- |
| 1    | Alice  | 100\$     | 1         |
| 2    | Bob    | 200\$     | 3         |

Два пользователя (А и Б) одновременно читают запись:

```sql
SELECT * FROM users WHERE id = 1;
```

Оба видят: `balance = 100$`, `version = 1`.



\<u>Пользователь А\</u> хочет списать 50\$:

```sql
UPDATE users SET balance = 50, version = version + 1 
WHERE id = 1 AND version = 1; -- Проверка версии!
```

Успех! Версия в БД была `1`, теперь станет `2`.



\<u>Пользователь Б\</u> (не зная об изменениях А) тоже хочет списать 30\$:

```sql
UPDATE users SET balance = 70, version = version + 1 
WHERE id = 1 AND version = 1; -- ОШИБКА! Версия уже 2!
```

СУБД не найдёт подходящей строки (т.к. `version` теперь `2`) и \<u>отменит операцию\</u>.



###### 🔹 Плюсы

✅ Нет блокировок при чтении — много пользователей могут одновременно читать данные.
✅ Высокая производительность — не нужно ждать, пока другая транзакция закончит работу.
✅ Подходит для редких конфликтов (например, интернет-магазины, где изменения одного товара разными людьми — редкость).



###### 🔹 Минусы

❌ Нужно переделывать работу при конфликте (например, если два менеджера редактируют заказ, один получит ошибку).
❌ Не подходит для частых конфликтов (например, банковские транзакции, где баланс меняется каждую секунду).



###### 🔹 Где применяется?

1. Веб-приложения (редактирование профилей, статей).
2. Кэширование (например, Redis).
3. Распределённые системы (когда данные копируются между серверами).



###### 🔹 Java Spring

\<u>Версионность\</u>. У сущности есть специальное поле (обычно `version`), которое увеличивается при каждом изменении записи.

```java
@Entity
public class MyEntity {
    @Id
    private Long id;
    
    @Version  // <- Вот это поле для оптимистичной блокировки
    private Long version;
    
    private String name;
}
```

\<u>Механизм проверки\</u>. Когда приложение читает сущность, оно запоминает текущий `version`. При обновлении Hibernate проверяет, что `version` в БД не изменился с момента чтения:

```sql
UPDATE my_entity SET name = 'new value', version = version + 1 
WHERE id = 1 AND version = 2;  -- Если version уже не 2, обновление не пройдет
```

Если `version` в БД изменился (значит, кто-то другой уже обновил запись), Hibernate бросает `ObjectOptimisticLockingFailureException`.



###### 🔹 ObjectOptimisticLockingFailureException: @GeneratedValue или ручная установка

Иногда возникает ситуация, когда нужно \<u>выбрать\</u> метод установки идентификатора сущности, иначе возникает исключение `ObjectOptimisticLockingFailureException`.



Рассмотрим пример где возникает подобное исключение. Модель `PurchaseEventJpa` хранит в себе связь с аттрибутами (модели `EventAttributeJpa`), они устанавливаются в `PurchaseEventJpa.from()`. Непосредственный конфликт взникает в модели `EventAttributeJpa`:

```java
@Entity
@Table(schema = "market", name = "event")
@Getter
@Setter
public class PurchaseEventJpa {
    @Id
    private UUID id;
    @Column(name="event_type")
    private String type;
    @Column(name="event_time")
    private ZonedDateTime timestamp;
    @Column(name="msisdn")
    private String msisdn;
    @OneToMany(mappedBy = "event", fetch = FetchType.EAGER, cascade = CascadeType.ALL)
    private Set<EventAttributeJpa> attributes;

    public static PurchaseEventJpa from(LoggableEvent event) {
        final PurchaseEventJpa jpa = new PurchaseEventJpa();
        jpa.setId(event.id());
        jpa.setMsisdn(event.msisdn());
        jpa.setType(event.type());
        jpa.setTimestamp(event.timestamp());
        jpa.setAttributes(event.attributes().stream().map(a -> EventAttributeJpa.from(a, jpa)).collect(Collectors.toSet()));
        return jpa;
    }

}
```

```java
@Entity
@Table(schema = "market", name = "attribute")
@Getter
@Setter
public class EventAttributeJpa {
    @Id
    @GeneratedValue
    private UUID id;
    @ManyToOne
    private PurchaseEventJpa event;
    @Column(name="attr_key")
    private String name;
    @Column(name="attr_value")
    private String value;

    public static EventAttributeJpa from(Attribute attr, PurchaseEventJpa event) {
        EventAttributeJpa jpa = new EventAttributeJpa();
        jpa.setId(attr.id());
        jpa.setEvent(event);
        jpa.setName(attr.key());
        jpa.setValue(attr.value());
        return jpa;
    }
}
```

**Как возникает ошибка:**

1. Этап 1: Сохранение `PurchaseEventJpa`
   * Вызовается `eventsRepository.save(PurchaseEventJpa.from(event))`.
   * Hibernate видит, что у `PurchaseEventJpa` есть каскадное сохранение атрибутов (`cascade = CascadeType.ALL`).
2. Этап 2: Обработка `EventAttributeJpa`
   * Для каждого атрибута Hibernate проверяет:
     * Если у объекта нет ID (или он `null`) → это новая сущность → выполни `INSERT`.
     * Если ID задан → это существующая сущность → попробуй `UPDATE`.
3. В нашем примере: ID задан вручную
   * Hibernate видит `jpa.setId(attr.id())` и думает:
     > "Ага, это существующая запись! Надо сделать. `UPDATE`"
   * Но в БД записи с таким ID ещё нет → Hibernate пытается обновить несуществующую строку.
4. Почему `ObjectOptimisticLockingFailureException`?
   * В новых версиях Spring Boot/Hibernate строже проверяется состояние сущностей.
   * При "обновлении" Hibernate также проверяет версию (даже если не используется `@Version`).
   * Так как записи нет, он считает, что её удалили или изменили в другой транзакции → отсюда и ошибка.



**Решение 1**: убрать `jpa.setId(attr.id())` из `EventAttributeJpa`

Если \<u>не задавать ID вручную\</u>, Hibernate считает сущность \<u>новой\</u>.

При сохранении выполняется `INSERT` с \<u>автоматической генерацией ID\</u> (как и задумано в `@GeneratedValue`).

```sql
-- Hibernate делает запрос сам:
INSERT INTO market.attribute (id, event_id, attr_key, attr_value) 
VALUES (gen_random_uuid(), 'event-uuid-123', 'region', 'Moscow');
```



**Решение 2**: убрать `@GeneratedValue` из `EventAttributeJpa`

Мы пытаетесь вручную задать `ID` (`jpa.setId(attr.id())`), но в `EventAttributeJpa` стоит `@GeneratedValue`. Теперь модель будет корректно сохранятся, т.к. ответственность за наличие ID лежит на "Вас".



###### 🔹 Как Hibernate решает: INSERT или UPDATE?

Механизм принятия решения зависит от стратегии генерации ID (аннотация `@GeneratedValue`). Разберём оба случая.



**С `@GeneratedValue` поведение Hibernate:**

Проверка ID:

* Если поле `id` не задано (`null`) → Hibernate считает сущность новой → делает `INSERT`.
* Если поле `id` задано → Hibernate считает сущность уже существующей → пытается сделать `UPDATE`.



**Без `@GeneratedValue` поведение Hibernate меняется:**

Проверка ID:

* Hibernate смотрит на аннотацию `@Id` без `@GeneratedValue` и понимает:

> Раз разработчик сам управляет ID, я не буду решать, новая это сущность или существующая. Пусть решает \<u>контекст сохранения\</u> (например, `persist()` или `merge()`)

Что происходит в коде?

Мы используем `eventsRepository.save()`. В Spring Data JPA этот метод:

* Сначала проверяет, есть ли сущность в БД (через `select` по `id`).
* Если нет → делает `INSERT`.
* Если есть → делает `UPDATE`.

Даже если вручную задается `id`, метод `save()` \<u>проверит\</u> его наличие в БД и корректно выполнит `INSERT`.



**Упрощённая логика метода `save()` в Spring Data JPA:**

```java
public <S extends T> S save(S entity) {
    if (entity.getId() == null) { 
        // Новый объект → INSERT
        em.persist(entity);
        return entity;
    } else {
        // ID задан → проверяем, есть ли в БД
        if (em.find(entity.getClass(), entity.getId()) != null) {
            // Обновляем существующий → UPDATE
            return em.merge(entity);
        } else {
            // ID задан, но записи нет → INSERT
            em.persist(entity);
            return entity;
        }
    }
}
```







###### 🔹 Чем отличается от пессимистичной блокировки?

| **Оптимистичная**                              | **Пессимистичная**                                              |
| ---------------------------------------------- | --------------------------------------------------------------- |
| Не блокирует записи на время чтения            | Блокирует запись при SELECT (например, `SELECT ... FOR UPDATE`) |
| Конфликты обнаруживаются только при обновлении | Конфликты предотвращаются заранее                               |
| Лучше для высоконагруженных систем             | Может вызывать дедлоки                                          |



###### 🔹 **Когда использовать?**

✅ **Оптимистичная блокировка:**

* Много чтения, мало записи.
* Нужна высокая производительность.
* Допустима повторная попытка при конфликте.

❌ **Пессимистичная блокировка:**

* Критично избежать одновременных изменений (например, банковские транзакции).
