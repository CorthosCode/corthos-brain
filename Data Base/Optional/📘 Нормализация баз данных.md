# 📘 Нормализация баз данных

**Нормализация** — это процесс преобразования структуры базы данных с целью устранения избыточности данных и аномалий (вставки, обновления, удаления).

Идея:

* Разбить большие "плоские" таблицы на более маленькие, связанные между собой.
* Достигается путём приведения таблиц к **нормальным формам**.





***

## Цели нормализации

* ✅ Уменьшить дублирование данных (минимизировать избыточность).
* ✅ Исключить аномалии (`INSERT`, `UPDATE`, `DELETE`).
* ✅ Упростить поддержку и модификацию данных.
* ✅ Повысить согласованность (одно изменение в БД обновляет все связанные данные автоматически).
* ✅ Сделать структуру более логичной и читаемой.





***

## Основные понятия

* **Функциональная зависимость (ФЗ)** — отношение между атрибутами:
  &#x20;если по значению атрибута `A` можно однозначно определить `B`, то `A → B`.
  &#x20;Пример: `student_id → student_name`.
* **Ключ (candidate key)** — минимальный набор атрибутов, который однозначно идентифицирует запись.
* **Первичный ключ (Primary Key)** — выбранный среди кандидатных ключей.
* **Нормальная форма (НФ)** — правила, которым должна удовлетворять таблица.





***

## Первая нормальная форма (1НФ)

**Правило:**

* Все атрибуты должны быть **атомарными** (неделимыми).
* Не должно быть повторяющихся групп и массивов.

❌ Пример (ненормализованная таблица):

| student\_id | name   | courses            |
| ----------- | ------ | ------------------ |
| 1           | Иванов | Математика, Физика |
| 2           | Петров | Химия              |

✅ Приведение к 1НФ:

| student\_id | name   | course     |
| ----------- | ------ | ---------- |
| 1           | Иванов | Математика |
| 1           | Иванов | Физика     |
| 2           | Петров | Химия      |





***

## Вторая нормальная форма (2НФ)

**Правило:**

* Таблица должна быть в **1НФ**.
* Все неключевые атрибуты должны **полностью зависеть** от **всего первичного ключа**, а не от его части.

❌ Пример (ключ составной: `(student_id, course)`):

| student\_id | course     | student\_name | dept\_name |
| ----------- | ---------- | ------------- | ---------- |
| 1           | Математика | Иванов        | Физика     |
| 1           | Физика     | Иванов        | Физика     |

Здесь:

* `student_name` и `dept_name` зависят только от `student_id`, а не от `(student_id, course)`.

✅ Решение: разделение на таблицы:

* `student(student_id, student_name, dept_name)`
* `enrollment(student_id, course)`





***

## Третья нормальная форма (3НФ)

**Правило:**

* Таблица должна быть во **2НФ**.
* Никакой **транзитивной зависимости**: неключевой атрибут не должен зависеть от другого неключевого атрибута.

❌ Пример:

| student\_id | student\_name | dept\_id | dept\_name |
| ----------- | ------------- | -------- | ---------- |
| 1           | Иванов        | 10       | Физика     |

Здесь:

* `dept_name` зависит от `dept_id`, а `dept_id` — от `student_id`.
* Значит, есть транзитивная зависимость: `student_id → dept_id → dept_name`.

✅ Решение:

* `student(student_id, student_name, dept_id)`
* `department(dept_id, dept_name)`





***

## НФ Бойса-Кодда (BCNF)

**Правило:**

* Для любой зависимости `X → Y`, множество `X` должно быть **ключом**.

Используется, когда даже в **3НФ** остаются аномалии.



**Пример (нарушение 3NF → нужно BCNF):**

```sql
Course   | Teacher
---------|---------
DB       | Ivanov
DB       | Petrov
Math     | Sidorov
```

* Ключ: **(Course, Teacher)** (т.к. один курс может вести несколько преподавателей).
* Но есть зависимость: `Teacher → Course` (каждый преподаватель ведёт только один курс).

👉 `Teacher` не суперключ, но определяет `Course`.

Значит, нарушается BCNF.

Нужно разделить таблицу:

```sql
Teacher   | Course
----------|---------
Ivanov    | DB
Petrov    | DB
Sidorov   | Math
```

```sql
Course
------
DB
Math
```





***

## Четвёртая нормальная форма (4НФ)

**Правило:**

* Таблица должна быть в **BCNF**.
* Не должно быть **многозначных зависимостей**.

Пример:

* У студента могут быть **несколько курсов** и **несколько языков**.
* Таблица: `(student, course, language)` → избыточность.

✅ Решение:

* `student_course(student, course)`
* `student_language(student, language)`





***

## Пятая нормальная форма (5НФ)

**Правило:**

* Таблица должна быть в **4НФ**.
* Убираются **аномалии соединений** (join anomalies).

📌 когда данные корректно выглядят только при разбиении на несколько таблиц, и все связи восстанавливаются только через **JOIN нескольких таблиц**.



Таблица в **5NF**, если:

> Любая непустая зависимость соединения (join dependency) — следствие **ключей**.

Или проще:
&#x20;👉 Таблицу нельзя без потерь разложить на меньшие таблицы **по соединениям**, кроме как по ключам.



```sql
Project   | Supplier   | Part
----------|------------|------
P1        | S1         | X
P1        | S1         | Y
P1        | S2         | X
```

* Проект `P1` использует детали `X, Y`.
* Поставщик `S1` поставляет `X, Y`.
* Поставщик `S2` — только `X`.



Если таблица хранит тройку `(Project, Supplier, Part)`, то возможны **избыточности и аномалии**.

Например:

* Добавим `(P1, S2, Y)` случайно — получится ложная комбинация (ведь `S2` не поставляет `Y`).



Решение (разбиение на три таблицы):

```sql
Project   | Part
P1        | X
P1        | Y
```

```sql
Project   | Supplier
P1        | S1
P1        | S2
```

```sql
Supplier  | Part
S1        | X
S1        | Y
S2        | X
```

👉 Теперь можно восстановить исходную таблицу через JOIN этих трёх.



**Когда применяется 5NF**

* Когда таблица описывает **многие-ко-многим-ко-многим** связи.
* Когда при разбиении на несколько таблиц нет потери информации (lossless join).
* Чаще встречается в **сложных моделях БД для ERP/SCM систем** (например, управление проектами, договорами, логистика).





***

## Денормализация

Иногда **сознательно нарушают нормализацию** ради:

* 🔹 Повышения производительности (меньше `JOIN`).
* 🔹 Ускорения аналитических запросов.

Пример: хранить `dept_name` прямо в `student`, хотя есть `dept_id`.





***

## Сравнение нормальных форм

| НФ       | Условие                                     |
| -------- | ------------------------------------------- |
| **1НФ**  | Атомарность, отсутствие повторяющихся групп |
| **2НФ**  | 1НФ + полная зависимость от ключа           |
| **3НФ**  | 2НФ + нет транзитивных зависимостей         |
| **BCNF** | Каждый детерминант — ключ                   |
| **4НФ**  | Нет многозначных зависимостей               |
| **5НФ**  | Нет аномалий соединений                     |



